{
  "name": "PHP undervisning",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [
    8000
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-pack"
      ],
      "settings": {
        "editor.formatOnSave": true
      }
    }
  },
  "postCreateCommand": "php -m | grep -q pdo_mysql && echo 'pdo_mysql already installed' || (echo 'pdo_mysql missing' && false) && php -v && composer --version"
}